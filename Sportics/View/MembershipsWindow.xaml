<Window x:Class="Sportics.View.MembershipsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
        xmlns:vm="clr-namespace:Sportics.ViewModel"
        xmlns:helper="clr-namespace:Sportics.Helper"
        mc:Ignorable="d"
        Title="Sportics"
        MinHeight="500" MinWidth="800"
        Width="1250" Height="650"
        WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:MembershipsViewModel />
    </Window.DataContext>

    <Window.Resources>
        <helper:ImageSourceConverter x:Key="ImageConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Верхняя панель -->
            <RowDefinition Height="*"/>
            <!-- Контент -->
            <RowDefinition Height="Auto"/>
            <!-- Нижняя панель -->
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <Grid Grid.Row="0" Background="WhiteSmoke" Height="60">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <!-- Лево: логотип -->
                <ColumnDefinition Width="Auto" />
                <!-- Центр: кнопки -->
                <ColumnDefinition Width="*" />
                <!-- Право: иконки -->
            </Grid.ColumnDefinitions>

            <!-- Левая часть: логотип и название -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="40,0" Grid.Column="0">
                <svg:SvgViewbox Source="/Resources/Images/dumbbell.svg" Height="40" Width="40" />
                <Label Content="SPORTICS" FontSize="24" VerticalAlignment="Center" Margin="10,0"/>
            </StackPanel>

            <!-- Центральная часть: кнопки -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Column="1">
                <Button Content="Таблицы" FontSize="16" Margin="10,0" Width="150" Command="{Binding OpenAdminCommand}"/>
                <Button Content="Добавить" FontSize="16" Margin="10,0" Width="150" Command="{Binding OpenAddMembershipCommand}"/>
            </StackPanel>

            <!-- Правая часть: иконки -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" Grid.Column="2">
                <Border Cursor="Hand" Margin="10">
                    <Border.InputBindings>
                        <MouseBinding Gesture="LeftClick" Command="{Binding }" />
                    </Border.InputBindings>
                    <svg:SvgViewbox Source="/Resources/Images/bell-regular.svg" Height="30" Width="30"/>
                </Border>

                <Border Cursor="Hand" Margin="10">
                    <Border.InputBindings>
                        <MouseBinding Gesture="LeftClick" Command="{Binding OpenAccountCommand}" />
                    </Border.InputBindings>
                    <svg:SvgViewbox Source="Resources\Images\circle-user-regular.svg" Height="30" Width="30"/>
                </Border>
            </StackPanel>
        </Grid>

        <!-- Основной контент -->
        <ScrollViewer Grid.Row="1" Margin="0">
            <WrapPanel Background="LightGray">
                <ListView ItemsSource="{Binding Memberships}" Width="auto">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Width="1510" ItemWidth="302"></WrapPanel>
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>

                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Background" Value="Transparent"/>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Width="250" Background="WhiteSmoke" Margin="18,10,18,10">
                                <StackPanel Margin="10">
                                    <Image Height="120" Stretch="Fill" Margin="5"
                                            Source="{Binding Photo, Converter={StaticResource ImageConverter}}"/>
                                    <TextBlock Text="{Binding ShortName}" FontSize="16" FontWeight="SemiBold" TextAlignment="Center" Margin="0,10"/>
                                    <TextBlock Text="{Binding Category}" FontSize="16" FontWeight="SemiBold" TextAlignment="Center" Margin="0,10"/>
                                    <TextBlock Text="{Binding Price}" FontSize="16" FontWeight="Bold" TextAlignment="Center" Margin="0,5"/>
                                    <Button Content="Подробнее" HorizontalAlignment="Center" Margin="0,10" Command="{Binding DataContext.DetailsCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding}"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </WrapPanel>
        </ScrollViewer>

        <!-- Нижняя панель -->
        <Border Grid.Row="2" Background="WhiteSmoke" Padding="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,15,0">
                <svg:SvgViewbox Source="Resources\Images\Mail.svg" Height="20" Width="20" Margin="0,0,5,0"/>
                <Label Content="sportics@gmail.com" VerticalAlignment="Center"/>
                <svg:SvgViewbox Source="Resources\Images\Phone.svg" Height="20" Width="20" Margin="20,0,5,0"/>
                <Label Content="+375 (44) 573-04-02" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
