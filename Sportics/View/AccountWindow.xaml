<Window x:Class="Sportics.View.AccountWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:Sportics.ViewModel"
        xmlns:helper="clr-namespace:Sportics.Helper"
        xmlns:local="clr-namespace:Sportics.View"
        mc:Ignorable="d"
        MinHeight="350" MinWidth="400"
        Title="" Height="400" Width="500" WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:AccountViewModel/>
    </Window.DataContext>

    <Grid Background="{DynamicResource MaterialDesignPaper}" Margin="20">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="450">
            <TextBlock Text="Личный кабинет" FontSize="24" FontWeight="SemiBold" TextAlignment="Center"/>
            <TextBlock Text="{Binding UserName}" FontSize="14" FontWeight="SemiBold" TextAlignment="Center" Margin="0,10"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10" HorizontalAlignment="Center">
                <TextBlock Text="{Binding UserBalance, StringFormat=Баланс: {0:C}}"
           FontSize="16" FontWeight="SemiBold" Margin="0,10"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Пополнить баланс" Height="35" Margin="0,15,5,0" Command="{Binding BalanceCommand}" Width="180"/>
                <Button Content="Выйти" Height="35" Margin="5,15,0,0" Command="{Binding ExitCommand}" Width="80"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
