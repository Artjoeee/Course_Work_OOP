<Window x:Class="Sportics.View.ReviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Sportics.ViewModel"
        mc:Ignorable="d"
        Title="Отзывы о тренере" Height="600" Width="700"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:ReviewViewModel />
    </Window.DataContext>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Таблица отзывов -->
        <DataGrid ItemsSource="{Binding Reviews}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  Grid.Row="0" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Пользователь" Binding="{Binding User.Name}" Width="150"/>
                <DataGridTextColumn Header="Комментарий" Binding="{Binding Comment}" Width="*"/>
                <DataGridTextColumn Header="Оценка" Binding="{Binding Rating}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Добавление отзыва -->
        <StackPanel Grid.Row="1" Margin="0" Orientation="Vertical">
            <TextBlock Text="Оставьте отзыв:" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding NewComment, UpdateSourceTrigger=PropertyChanged}" 
                     AcceptsReturn="True" Height="60" TextWrapping="Wrap"/>
            <TextBlock Text="{Binding CommentValidationError}" Foreground="Red" FontSize="12"/>

            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <TextBlock Text="Оценка:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox Width="80" SelectedItem="{Binding NewRating}">
                    <ComboBoxItem Content="1" />
                    <ComboBoxItem Content="2" />
                    <ComboBoxItem Content="3" />
                    <ComboBoxItem Content="4" />
                    <ComboBoxItem Content="5" />
                </ComboBox>
                <TextBlock Text="{Binding RatingValidationError}" Foreground="Red" FontSize="12" Margin="10,0,0,0"/>
            </StackPanel>

            <Button Content="Оставить отзыв" Width="150" Height="30" Margin="0,10,0,0"
                    Command="{Binding SubmitReviewCommand}" IsEnabled="{Binding CanSubmit}" HorizontalAlignment="Left"/>
        </StackPanel>
    </Grid>
</Window>
